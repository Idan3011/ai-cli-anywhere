# ============================================================
# TELEGRAM
# ============================================================

# Bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=123456789:ABCDefGhIJKlmNoPQRsTUVwxyZ

# Your personal Telegram numeric chat ID (find it via @userinfobot)
# Only this chat ID may interact with the bot.
ALLOWED_CHAT_ID=987654321

# ============================================================
# AI ROUTING
# ============================================================

# Claude CLI — install from https://claude.ai/code
# Command to invoke it (usually just "claude")
CLAUDE_CLI_PATH=claude

# Max seconds to wait for Claude CLI response
CLAUDE_TIMEOUT=45

# Cursor Agent CLI — the "agent" subcommand of the Cursor CLI
# Install Cursor and ensure the CLI is in PATH
# Leave blank or remove to disable Cursor routing (all messages go to Claude)
CURSOR_CLI_PATH=agent

# Max seconds to wait for Cursor Agent response
CURSOR_TIMEOUT=60

# Directory Cursor Agent will work in (your project root)
# Leave blank to use the directory where the bot is started
CURSOR_WORKING_DIR=/home/user/myproject

# Phrases that route a message to Claude CLI instead of Cursor (comma-separated, case-insensitive)
CLAUDE_PATTERNS=@claude,claude:,hey claude,claude,

# Short aliases for Claude models, used with the /model claude command.
# Format: alias:full-model-id  (comma-separated, no spaces around colons/commas)
# When Anthropic releases a new model version, just update the right-hand side here and restart.
# Unrecognised values are passed through as-is, so full model IDs always work too.
CLAUDE_MODEL_ALIASES=opus:claude-opus-4-6,sonnet:claude-sonnet-4-5-20250929,haiku:claude-haiku-4-5-20251001

# ============================================================
# LOGGING
# ============================================================
LOG_LEVEL=INFO
